{
  "name": "theme",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "release": "bun tasks/release.js",
    "dev-build": "bun tasks/build.js --all",
    "dev-watch": "bun tasks/watch.js --all",
    "build": "bun tasks/build.js --js --css",
    "watch": "bun tasks/watch.js --js --css",
    "debug": "bun tasks/debug.js",
    "lint": "biome lint --write",
    "format": "biome format --write",
    "check": "biome check --fix --unsafe"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "chokidar": "^4.0.3",
    "esbuild": "^0.25.0",
    "esbuild-plugin-external-global": "^1.0.1",
    "sass": "^1.85.0",
    "ws": "^8.18.1"
  }
}
